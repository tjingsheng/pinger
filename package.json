{
  "name": "pinger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --platform=node --target=node22 --format=esm --bundle --minify --legal-comments=none --outdir=dist --out-extension:.js=.mjs src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
