{
  "name": "pinger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --platform=node --target=node16 --bundle --minify --legal-comments=none --outdir=dist --out-extension:.js=.cjs src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
